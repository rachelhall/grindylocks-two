import React from "react";

import styles from "./AccountCard.module.scss";

interface IProps {

}

export const AccountCard: React.FC<IProps> = (props) => {
    const { } = props;

    return (
        <div className={ styles.AccountCard }>

        </div>
    );
};

export default AccountCard;
